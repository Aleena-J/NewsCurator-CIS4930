export class Article {
  constructor({ id, title, description, url, date }) {
    this.article_id = id;
    this.title = title;
    this.description = description;
    this.source = source;
    this.url = url;
    this.date = date;
  }
}
